import { ClubFilterPipe } from './club-filter.pipe';

describe('ClubFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClubFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
