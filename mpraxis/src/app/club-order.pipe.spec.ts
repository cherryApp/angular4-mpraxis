import { ClubOrderPipe } from './club-order.pipe';

describe('ClubOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ClubOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
